version: '3.4'

services:
  catalogdb:
    image: postgres:17.0

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: Services/Catalog/Catalog.API
      dockerfile: Dockerfile

volumes:
  postgres_catalog:
      
